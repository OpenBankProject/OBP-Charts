apiVersion: v1
kind: Service
metadata:
    name: {{ .Release.Namespace }}-portal-service
    namespace: {{.Release.Namespace}}
spec:
    selector:
        app: {{.Release.Namespace}}-portal
    ports:
    - name: http
      protocol: TCP
      port: 85
      targetPort: 3000
    type: ClusterIP
